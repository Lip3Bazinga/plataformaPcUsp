**Back**
  # Get all notifications, update notification status
  # Delete read notifications -- with cron after a certain time
  # Get all users, courses, orders
  # Add and get members in Admin dashboard
  # Delete course, Delete user
  # Get the last 28 days' users, orders, notifications for the last 1 year
  # Layout model Design
  # Create Faq, Hero banner, Categories
  # Edit Faq, Hero banner, Categories
  # Advance cache maintenance

**Front**
  # Profile UI + Functionalites
  # Edit Profile + Update Password
  # Admin SideBar + Course 
  # Course content UI and course preview UI
  # Create Course Feature
  # Get all users, courses, manage team
  # Add new user in Admin Dashboard
  # Edit & Delete Course
  # Banner, FAQ, Categories Customization

# **Project Overview** (`Final steps, fixing all bugs and Refactoring and optimization`)
  # Project Overview
  # Software Design / Project Plan
  # Technology brief
  # Code Editor overview
  # Project server setup
  # Database, Cloudinary, Redis connections
  # Handle Errors
  # User model design
  # User registration
  # User Activation
  # User login, logout
  # User protected & Authorization middleware
  # Generate new Access token, user social authentication, get user info
  # Update user info, password, avatar
  # Course Model design
  # Create course, Edit course
  # Get single and all courses -- without purchasing
  # Get course content, get user accessible all courses --- only for valid-user
  # Create question, add reply in question
  # Add review in course
  # Notification and Order model
  # Create Order

**Deploy**

# Deploy do Back-end (API e banco de dados).
# Deploy do Front-end (Next.js no Vercel ou outra plataforma).
# Testes pós-deploy:
# Realize testes de produção, incluindo navegação no site, interações com APIs e verificações de segurança.
# Monitoramento:
# Configure ferramentas de monitoramento (ex.: Sentry para erros e LogRocket para performance) para garantir o funcionamento contínuo do sistema.